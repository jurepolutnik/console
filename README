CONSOLE SETUP
----------------

map <C-t> :tabnew<CR>

inoremap <C-Tab> <Esc>:tabnext<CR>
inoremap <C-S-Tab> <Esc>:tabprevious<CR>
inoremap <C-t> <Esc>:tabnew<CR>

ZSH, TMUX, VIM

Install TMUX
===============

sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

Vim default editor - sudo  update-alternatives --config editor

Setup
==============
git submodule init && git submodule update
git clone --recursive ...

make install
cd dotvim && make install


nnoremap <C-S-j> :tabprevious
nnoremap <C-S-k> :tabnext
